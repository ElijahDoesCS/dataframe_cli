#include <stdio.h>

 convert_index(const char *index) {

}

__attribute__((visibility("default"))) int load_data(const char *file_name, const char *rows, const char *cols) {
    printf("Filename: %s\n", file_name);
    printf("Rows: %s\n", rows);
    printf("Columns: %s\n", cols);

    // Open and interpret the csv file, grab its contents from the specified range, 
    // export it as a matrix for operation with alternate libraries
    
    int starting_column, ending_column, starting_row, ending_row;



    return 0;
} 


